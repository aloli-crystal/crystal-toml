= Journal des modifications
:toc: left
:toc-title: Versions
:toclevels: 2

== v0.1.1 — 2026-04-29

=== Modifié

* **Repo GitHub renommé** `aloli-crystal/toml` → `aloli-crystal/toml`
  (convention de nommage Aloli : les créations originales abandonnent
  le préfixe `crystal-`). Les anciens liens redirigent automatiquement.
* Champ `name:` du `shard.yml` mis à jour de `toml` à `toml`.
* Fichier d'entrée renommé `src/toml.cr` → `src/toml.cr`.
  Côté consommateur, `require "toml"` devient `require "toml"`.
* Cible binaire du décodeur renommée `toml-decoder` →
  `toml-decoder` (`shards build toml-decoder` → `bin/toml-decoder`).

== Non publié

Documentation :

* README bilingue détaillé (EN + FR) couvrant installation, les
  deux surfaces d'API, lecture typée, édition, préservation au
  round-trip, table des types TOML → Crystal, migration depuis
  `crystal-community/TOML.cr`, limitations et vue d'ensemble de
  l'architecture.
* Trois exemples exécutables dans `examples/` illustrant le
  drop-in `parse_to_hash`, une édition de coffre de secrets
  annoté, et la lecture typée par path pointé.
* Documentation API générée par `crystal docs` et déployée sur
  https://aloli-crystal.github.io/toml/ à chaque push
  sur `production` (workflow GitHub Pages).

== v0.1.0 — 2026-04-28

Première version publique de `toml`. Parser et sérialiseur
TOML v1.0 en Crystal pur, avec préservation des commentaires et de
la mise en forme lors d'un round-trip
`parse → modification → écriture`.

API publique
------------

* `TOML.parse(string) : TOML::Document` — parsing vers un AST
  riche qui préserve commentaires, lignes vides et formatage
  byte-level d'origine. Round-trip byte-identique pour input non
  modifié.
* `TOML.parse_to_hash(string) : Hash(String, TOML::Type)` —
  remplaçant compatible de `crystal-community/TOML.cr`. Perd
  les trivia, retourne un hash plat.
* Lecture typée sur `Document` : `#string?`, `#int?`,
  `#float?`, `#bool?`, `#datetime?`, `#time_of_day?` (et
  variantes sans `?` qui lèvent une erreur si absent),
  `#has_key?`, `#get?`, `#get`.
* Édition sur `Document` : `#set(key, value)`,
  `#set_with_comment(key, value, comment)`, `#delete(key)` —
  préserve le formatage de la ligne autour à la mise à jour.

Conformité
----------

**Conformité totale avec TOML v1.0**, mesurée par la suite
officielle d'interop `toml-lang/toml-test` v2.1.0 :

* Tests valid   : **205 / 205** (100 %)
* Tests invalid : **473 / 473** (100 %)
* Total        : **678 / 678** (100 %)

Le binaire decoder `bin/toml-decoder` est construit dans
la CI et la suite est lancée à chaque push sur la branche
`production`.

Voir `CRYSTAL-TOML-SPECS.adoc` (dans le repo voisin `prod-crystal/`)
pour la spécification complète.
